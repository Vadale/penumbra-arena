name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Python — pytest + pyright + ruff
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      # Pin Python 3.13: tenseal 0.3.x only ships cp312/cp313 wheels,
      # and the runner's homebrew default is now 3.14. uv will download
      # the matching python-build-standalone if it isn't already.
      - name: Sync workspace
        run: uv sync --all-packages --python 3.13

      - name: Ruff check
        run: uv run ruff check packages

      - name: Pyright (strict)
        run: uv run pyright packages

      - name: Pytest
        env:
          PENUMBRA_SEED: "42"
        run: uv run pytest packages -k "not slow" -q

  frontend:
    name: TS — typecheck + biome + vitest + build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install pnpm
        run: corepack enable && corepack prepare pnpm@latest --activate

      - name: Install deps
        working-directory: apps/web
        run: pnpm install --frozen-lockfile=false

      - name: Typecheck
        working-directory: apps/web
        run: pnpm typecheck

      - name: Biome check
        working-directory: apps/web
        run: pnpm exec biome check src

      - name: Vitest
        working-directory: apps/web
        run: pnpm test

      - name: Build
        working-directory: apps/web
        run: pnpm build
